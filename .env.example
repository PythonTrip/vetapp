# FastAPI (apps/api) — required
DATABASE_URL="postgresql://USER:PASSWORD@127.0.0.1:15432/vet_app"
INSTANCE_PASSWORD="change-me"

# Next.js UI (frontend/) — FastAPI origin
# Also read from the repo-root .env via frontend/next.config.ts
NEXT_PUBLIC_API_URL="http://127.0.0.1:8000"

# Lesion photos and documents (metadata stays in PostgreSQL)
ATTACHMENT_DIR="data/attachments"
