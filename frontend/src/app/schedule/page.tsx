import { AppShell } from "@/components/app-shell";
import { ScheduleBoard } from "@/components/clinical/schedule-board";

export default function SchedulePage() {
  return (
    <AppShell>
      <ScheduleBoard />
    </AppShell>
  );
}
